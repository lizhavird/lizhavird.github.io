var yourName= prompt ("What is your name?")
var yourAnswer= prompt (`Do ${yourName} know how to write code?`)
    if (yourAnswer === "yes") {
        alert (`You wil rule the world ${yourName}!`)}
    else {
        alert ("Well, good luck with that.")
        }
